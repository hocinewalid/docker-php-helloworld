apiVersion: v1
kind: Service
metadata:
    name: hocinewaliddockerphphelloworld
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: hocinewaliddockerphphelloworld